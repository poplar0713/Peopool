<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.peopool.model.repo.SuggestionRepo">

	<insert id="registerSuggestion" parameterType="Suggestion">
		insert into suggestion(sug_send, sug_timeone, sug_timetwo, sug_timethree, ind_index, ent_index, sug_state, sug_duty) 
		values(now(), #{sug_timeone}, #{sug_timetwo}, #{sug_timethree}, #{ind_index}, #{ent_index}, "W", #{sug_duty})
	</insert>
	
	<update id="acceptSuggestion" parameterType="Suggestion">
		update suggestion 
		set sug_decision = #{sug_decision}, sug_state = 'T' 
		where sug_index = #{sug_index} 
	</update>
	
	<update id="rejectSuggestion" parameterType="int">
		update suggestion 
		set sug_decision = null, sug_state = 'F' 
		where sug_index = #{sug_index} 
	</update>
	
	<delete id="deleteSuggestion" parameterType="int">
		delete from suggestion 
		where sug_index = #{index}
	</delete>
	
	<select id="getSuggestion" parameterType="int" resultType="Suggestion">
		select * 
		from suggestion 
		where ind_index = #{index}
	</select>
	
	<select id="getEntSuggestion" parameterType="int" resultType="Suggestion">
		select * 
		from suggestion 
		where ent_index = #{index}
	</select>
</mapper>
